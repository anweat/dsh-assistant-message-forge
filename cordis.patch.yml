# dsh-assistant-message-forge bundle patch —— 挂进 profile 插件树。
# Host 半注册 /dsh-assistant-message-forge 通用 RPC 通道；
# client-modules 扫描器通过本行的包名发现 dsh.client 声明并加载 lib/client.js。
- insert:
    - id: dsh-assistant-message-forge
      name: dsh-assistant-message-forge
      config: {}
